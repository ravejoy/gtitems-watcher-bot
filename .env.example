BOT_TOKEN=                     # Telegram bot token (from BotFather)

UPSTASH_REDIS_REST_URL=        # Upstash Redis REST API URL (https://xxx.upstash.io)
UPSTASH_REDIS_REST_TOKEN=      # Upstash Redis REST API token (secret from Upstash console)

BASE_URL=                      # Target site base URL
ITEMS_CGI_VER=                 # Items endpoint version
ITEMS_CGI_PATH_TEMPLATE=       # CGI path template for items

WEBHOOK_SECRET=                # Optional: secret for webhook verification (Vercel/Render)
